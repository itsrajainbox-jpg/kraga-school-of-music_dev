name: Verify FTP deployed content
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check current index.html on server
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASS }}
        run: |
          python3 - <<'PY'
          import os, ftplib, io
          ftp=ftplib.FTP(os.environ['FTP_HOST'], timeout=30)
          ftp.login(os.environ['FTP_USER'], os.environ['FTP_PASS'])
          ftp.cwd('/public_html')
          data=io.BytesIO()
          ftp.retrbinary('RETR index.html', data.write)
          ftp.quit()
          html=data.getvalue().decode('utf-8','replace')
          print('HAS_TEST:', 'across the world' in html)
          import re
          m=re.search(r'Proudly teaching students online across[^<]*', html)
          print('HEADING:', repr(m.group(0)) if m else 'NOT_FOUND')
          print('LEN:', len(html))
          # show first 500 chars of heading area
          m2=re.search(r'Proudly teaching students online across.*?</h3>', html, re.DOTALL)
          print('CONTEXT:', m2.group(0)[:200] if m2 else 'NO MATCH')
          PY
